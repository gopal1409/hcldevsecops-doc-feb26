##to do continues deployment in k8s we need to have all the manifest file in our repo open powershell
  - -----------
    cd \
    git clone https://github.com/gopal1409/skyproject-gitops.git
    cd .\skyproject-gitops\
  #as you are cloning my repo we are deleting some refrences of git
   rm .git
    git init
    git add .
    git commit -m "kubernetes-manifest"
    git branch -M main
  #replace this remote repo with your own repo so create a new repo 
    git remote add origin 
  #finally push it in github
   git push -u origin main
##lets generate the token so that my ci project can authentication with cd project or gitops project
##open github setting using the link 
https://github.com/settings/tokens
##########once opened click on 
generate new token-->generate new token(classic)
###give a name to the token
gitops-repo-access
##################
##select the scopes
repo
workflow
write:packages
####then generate the token and store it in a note pad
#then go back to your ci repo 
##cllick on setting of the repo
###in setting go below you will find security
click on secret and variables
#####then click on new repo secret
#name will be 
GITOPS_TOKEN
###paste the token which you got it from previous step
